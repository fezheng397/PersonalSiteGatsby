const radiusBase = 2;

export const Sizes = {
  // Border Radius
  radius1: `${radiusBase}px`,
  radius2: `${radiusBase * 2}px`,
  radius4: `${radiusBase * 4}px`,
  radius5: `${radiusBase * 5}px`,
  radiusCircle: '50%',
};
